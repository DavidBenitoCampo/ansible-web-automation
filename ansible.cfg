[defaults]
inventory = inventory/docker.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = keys/ansible_id_rsa
roles_path = roles
retry_files_enabled = False
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
