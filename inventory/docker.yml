all:
  children:
    webservers:
      hosts:
        web1:
          ansible_host: localhost
          ansible_port: 2221
        web2:
          ansible_host: localhost
          ansible_port: 2222
    databases:
      hosts:
        db1:
          ansible_host: localhost
          ansible_port: 2223

  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ./keys/ansible_id_rsa
    ansible_host_key_checking: false
